<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions required for SMS handling as in the lab images -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab4">
        <activity
            android:name=".MenuActivity"
            android:exported="true"
            android:label="@string/menu_title"
            android:theme="@style/Theme.Lab4">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".Lab06_1Activity"
            android:exported="false"
            android:label="@string/lab06_1"
            android:theme="@style/Theme.Lab4" />
        
        <activity
            android:name=".PowerStateChangeActivity"
            android:exported="false"
            android:label="@string/lab06_2"
            android:theme="@style/Theme.Lab4" />
        
        <activity
            android:name=".Lab06_3Activity"
            android:exported="false"
            android:label="@string/lab06_4"
            android:theme="@style/Theme.Lab4" />
        
        <!-- Register Power state change receiver (declared in manifest so it listens even if app is stopped) -->
        <receiver android:name=".PowerStateChangeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
                <action android:name="android.intent.action.ACTION_POWER_DISCONNECTED" />
            </intent-filter>
        </receiver>

        <!-- Register SmsReceiver to listen for incoming SMS messages -->
        <receiver android:name=".SmsReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>
        
        <!-- Test receiver to verify SMS broadcast is received -->
        <receiver android:name=".TestSmsReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>